server:
  port: 1999
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/youzhigithub/springcloud-config
          search-paths: /**
          username:
          password:
      label: master